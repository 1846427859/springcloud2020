server:
  port: 8001
spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://192.168.1.211:3306/db2019?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: LYC19951109...
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.springcloud.entities

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka
#      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
  instance:
    instance-id: payment8001
    prefer-ip-address: true
    # 8001 给 7001注册中心发心跳包的间隔时间 默认30秒
    lease-renewal-interval-in-seconds: 1
    # 7001 最后一次接收心跳包后，等待下一次心跳包的等待时间上限，超时剔除服务 默认90秒
    lease-expiration-duration-in-seconds: 3
